# Django
SECRET_KEY=replace-me-with-a-long-random-string-at-least-50-chars
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_NAME=neondb
DB_USER=stevenmartinez
DB_PASSWORD=replace-me-with-stevenmartinez-password
DB_HOST=ep-wispy-cell-ay3uv4fv-pooler.c-5.us-east-2.aws.neon.tech
DB_PORT=5432
DB_SSLMODE=require

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:8080

# Email (SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=replace-me
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=Tienda <noreply@example.com>

# Frontend URL (for password reset links, etc.)
FRONTEND_URL=http://localhost:3000
